#ifndef INC_5_COLORATION_UTILS_HPP
#define INC_5_COLORATION_UTILS_HPP

#include <vector>

/**
 * Utility function used to create a vector which contains integers from "start" to "to"
 * @param start The lower bound value (included)
 * @param to The upper bound value (excluded)
 * @return A vector of integers
 */
std::vector<int> range(int start, int to);

#endif
